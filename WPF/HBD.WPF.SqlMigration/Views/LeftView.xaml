<UserControl x:Class="HBD.WPF.SqlMigration.Module.Views.LeftView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:HBD.WPF.Controls;assembly=HBD.WPF"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.ContentTemplate>
        <DataTemplate>
            <controls:HeaderedContentGridControl>
                <controls:HeaderedContentGridControl.Header>
                    <Border x:Name="Border" Height="30" Background="Aqua">
                        <TextBlock>Hello</TextBlock>
                    </Border>
                </controls:HeaderedContentGridControl.Header>

                <TextBlock>Information</TextBlock>
            </controls:HeaderedContentGridControl>
        </DataTemplate>
    </UserControl.ContentTemplate>
</UserControl>